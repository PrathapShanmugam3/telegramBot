{
  "name": "telegram-bot-backend",
  "version": "1.0.0",
  "description": "Telegram bot backend for Vercel",
  "main": "api/bot.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "telegraf": "^4.12.0"
  },
  "engines": {
    "node": ">=14"
  }
}
